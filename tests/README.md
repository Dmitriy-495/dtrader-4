# 🧪 Тесты dtrader-4

## 📋 Содержание

### Тестовые скрипты
- [📄 final_test.js](final_test.js) - Полный тест миграции конфигурации
- [📄 test_config.js](test_config.js) - Тест базовой конфигурации

## 🚀 Как запускать тесты

### Полный тест миграции:
```bash
# Запустить полный тест
node final_test.js

# Ожидаемый результат: все тесты пройдены ✅
```

### Тест базовой конфигурации:
```bash
# Запустить тест конфигурации
node test_config.js

# Ожидаемый результат: конфигурация валидна ✅
```

## 📖 Структура тестов

```
tests/
├── README.md              # Этот файл
├── final_test.js          # Полный тест миграции
└── test_config.js         # Тест конфигурации
```

## 🧪 Что проверяют тесты

### final_test.js
Проверяет:
1. ✅ Структуру файлов конфигурации
2. ✅ Отсутствие дублирующихся .env файлов
3. ✅ Содержимое базового конфига
4. ✅ Корректность импортов в инстансах
5. ✅ Настройки docker-compose.yml
6. ✅ Переменные в корневом .env

### test_config.js
Проверяет:
1. ✅ Существование базового конфига
2. ✅ Существование корневого .env
3. ✅ Отсутствие дублирующихся .env в инстансах

## 🔧 Рекомендации

1. **Запускайте тесты регулярно**: Особенно после изменений в конфигурации
2. **Добавляйте новые тесты**: При добавлении новых функций или конфигов
3. **Интегрируйте в CI/CD**: Добавьте запуск тестов в процесс сборки

## 📝 Пример интеграции в CI/CD

```yaml
# Пример для GitHub Actions
jobs:
  test:
    name: Run configuration tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Run config tests
        run: node tests/final_test.js
```

## 💡 Советы

- **Перед коммитом**: Запустите `node tests/final_test.js`
- **После изменений**: Проверьте, что все тесты проходят
- **Для отладки**: Используйте `test_config.js` для быстрой проверки

При возникновении проблем с тестами обращайтесь к документации или создавайте issues в репозитории.