# 🚀 Запуск и остановка инстансов

## 📋 Варианты запуска

### 1. Запуск через npm

**Для разработки:**
```bash
# Запустить bot
cd bot && npm run start:dev

# Запустить trader
cd trader && npm run start:dev

# Запустить ws-server
cd ws-server && npm run start:dev
```

**Для продакшена:**
```bash
# Запустить bot
cd bot && npm run start:prod

# Запустить trader
cd trader && npm run start:prod

# Запустить ws-server
cd ws-server && npm run start:prod
```

### 2. Запуск через docker-compose

**Для разработки:**
```bash
# Запустить все инстансы
cd dtrader-4 && docker-compose up -d

# Остановить все инстансы
cd dtrader-4 && docker-compose down
```

**Для продакшена:**
```bash
# Запустить все инстансы
cd dtrader-4 && docker-compose -f docker-compose.prod.yml up -d

# Остановить все инстансы
cd dtrader-4 && docker-compose -f docker-compose.prod.yml down
```

### 3. Запуск через PM2

**Для продакшена:**
```bash
# Установить PM2
npm install -g pm2

# Запустить bot
cd bot && pm2 start dist/app.js --name dtrader-bot

# Запустить trader
cd trader && pm2 start dist/app.js --name dtrader-trader

# Запустить ws-server
cd ws-server && pm2 start dist/app.js --name dtrader-ws-server

# Просмотреть статус
pm2 list

# Остановить все
pm2 stop all

# Удалить все
pm2 delete all
```

## 🔧 Управление инстансами

### Просмотр логов

**Для docker-compose:**
```bash
# Просмотреть логи всех инстансов
cd dtrader-4 && docker-compose logs -f

# Просмотреть логи конкретного инстанса
cd dtrader-4 && docker-compose logs -f bot
```

**Для PM2:**
```bash
# Просмотреть логи
pm2 logs

# Просмотреть логи конкретного инстанса
pm2 logs dtrader-bot
```

### Перезапуск инстансов

**Для docker-compose:**
```bash
# Перезапустить все инстансы
cd dtrader-4 && docker-compose restart

# Перезапустить конкретный инстанс
cd dtrader-4 && docker-compose restart bot
```

**Для PM2:**
```bash
# Перезапустить все инстансы
pm2 restart all

# Перезапустить конкретный инстанс
pm2 restart dtrader-bot
```

### Обновление инстансов

**Для docker-compose:**
```bash
# 1. Остановить инстансы
cd dtrader-4 && docker-compose down

# 2. Обновить код
# ...

# 3. Пересобрать образы
cd dtrader-4 && docker-compose build

# 4. Запустить инстансы
cd dtrader-4 && docker-compose up -d
```

**Для PM2:**
```bash
# 1. Остановить инстансы
pm2 stop all

# 2. Обновить код
# ...

# 3. Пересобрать
cd bot && npm run build
cd trader && npm run build
cd ws-server && npm run build

# 4. Перезапустить
pm2 restart all
```

## 📊 Мониторинг инстансов

### Для docker-compose
```bash
# Просмотреть статус
cd dtrader-4 && docker-compose ps

# Просмотреть использование ресурсов
cd dtrader-4 && docker stats
```

### Для PM2
```bash
# Просмотреть статус
pm2 list

# Просмотреть использование ресурсов
pm2 monit
```

## 🚀 Рекомендации

### Для разработки:
- Используйте `npm run start:dev` для запуска с hot-reload
- Используйте `docker-compose up -d` для запуска всех инстансов
- Используйте `docker-compose logs -f` для просмотра логов

### Для продакшена:
- Используйте `docker-compose -f docker-compose.prod.yml up -d` для запуска
- Используйте `pm2` для управления процессами
- Используйте `docker stats` для мониторинга ресурсов

### Для отладки:
- Используйте `docker-compose logs -f` для просмотра логов
- Используйте `docker exec -it <container> bash` для входа в контейнер
- Используйте `pm2 logs` для просмотра логов

## 📖 Документация

- [RUNNING_INSTANCES.md](RUNNING_INSTANCES.md) - Это документ
- [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) - Руководство по интеграции
- [ARCHITECTURE_FIX.md](ARCHITECTURE_FIX.md) - Исправление архитектуры

## 🙏 Заключение

**Документация по запуску и остановке инстансов успешно создана!** Теперь:

✅ **Запуск**: Легко запускать инстансы
✅ **Остановка**: Легко останавливать инстансы
✅ **Мониторинг**: Легко мониторить инстансы
✅ **Обновление**: Легко обновлять инстансы

**Все задачи по документации выполнены успешно! 🎉**

При возникновении вопросов или необходимости доработок обращайтесь к документации в каталоге `docs/` или создавайте issues в репозитории.