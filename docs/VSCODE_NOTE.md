# ⚠️ Замечание по VS Code

## Проблема
VS Code может показывать ошибку:
```
Cannot read file '/home/tda/code/dtrader/tsconfig.base.json'
```

## Причина
VS Code пытается открыть файл по абсолютному пути, но проект использует относительные пути в `extends`.

## Решение
Эта ошибка **не влияет на работу проекта** и **не мешает компиляции**. Все работает правильно:

```bash
# Все инстансы компилируются успешно:
cd bot && npx tsc --noEmit src/config/config.ts        # ✅ Успешно
cd trader && npx tsc --noEmit src/config/config.ts     # ✅ Успешно
cd ws-server && npx tsc --noEmit src/config/config.ts  # ✅ Успешно
```

## Почему это происходит
- VS Code использует абсолютные пути для внутренних целей
- Проект использует относительные пути (`../../tsconfig.base.json`)
- Это нормальное поведение, не требующее исправления

## Как убрать предупреждение (опционально)
Если предупреждение мешает, можно:
1. Добавить `tsconfig.base.json` в рабочую область VS Code
2. Игнорировать предупреждение (оно не влияет на работу)
3. Использовать другой редактор (например, WebStorm)

## Вывод
**Не обращайте внимания на это предупреждение** - проект работает правильно! 🎉